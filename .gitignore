# Full raw dataset (203MB) — too large for a normal git push.
# Use Git LFS, cloud storage, or a Snowflake stage instead (see data/README.md).
fmcg_sales_3years_1M_rows.csv
data/fmcg_sales_3years_1M_rows.csv

# OS / editor junk
.DS_Store
Thumbs.db
*.tmp

# Power BI local cache files (if any get generated)
*.pbix.bak
~$*.pbix
